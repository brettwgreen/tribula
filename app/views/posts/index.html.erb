
<h1>Listing posts</h1>

<%= link_to 'New Post', new_post_path %>

<table>
  <tr>
    <th>Post</th>
  </tr>

  <% @posts.each do |post| %>
      <tr>
        <td><%= post.content%></td>
        <td><%= link_to 'View', post_path(post) %></td>
      </tr>
  <% end %>
</table>